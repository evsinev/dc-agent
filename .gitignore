classpath
.project
.settings/
target/
*.iml
*.ipr
*.iws
*~
.idea
gen/
classes/
gen-external-apklibs/
.vertx
.DS_Store
/config
/opt
deploy*.sh
install-*.sh
test-*.sh
dependency-reduced-pom.xml

# OWASP dependency-check runner — holds live API keys/tokens, keep local only
check-owasp.sh

dc-agent-operator/target-assets/

# Local verification harness runtime state (config git repo, jars logs, pids)
/verify/.run/

/handoffs/

.sonar-token

/tools/

# On-demand code-quality reports (Checkstyle / Spotless / Error Prone) — see tools/*-report.sh
/build-reports/
